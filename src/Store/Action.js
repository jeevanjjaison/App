export const setUserName = (userName) => ({
    type: 'SET_USERNAME',
    payload: userName,
  });

  export const setTask = (task) => ({
    type: 'SET_TASK',
    payload: task,
  });

  export const setAtask = (atask) => ({
    type: 'SET_ATASK',
    payload: atask,
  });

  export const setUsers = (users) => ({
    type: 'SET_USERS',
    payload: users,
  });

  export const setStatus = (status) => ({
    type: 'SET_STATUS',
    payload: status,
  });
    
    
    
  